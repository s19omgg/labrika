import {useState} from 'react';
import {ArrowRight,Check,Eye,EyeOff,Layers3,LoaderCircle} from 'lucide-react';
import {loginAccount,registerAccount} from './billing-data';
import LegalDocuments from './LegalDocuments';
import type {LegalDocument} from './LegalDocuments';
export default function LabrikaEntry(){
 const [mode,setMode]=useState<'signup'|'login'>('signup'),[name,setName]=useState(''),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[visible,setVisible]=useState(false),[busy,setBusy]=useState(false),[error,setError]=useState(''),[legal,setLegal]=useState<LegalDocument|null>(null);
 return <><main className="design-v2 labrika-entry entry-v6"><section className="labrika-entry-story"><div className="labrika-entry-brand"><span>L</span>labrika.</div><h1>Хорошие идеи<br/>заслуживают<br/><em>результата.</em></h1><div className="entry-story-art" aria-hidden="true"><div className="entry-art-card"><span>От идеи к публикации</span><Layers3 size={39}/><div><i/><i/><i/></div><strong>Одна система.<br/>Все площадки.</strong></div><span className="entry-art-orbit"/><span className="entry-art-check"><Check size={30}/></span></div></section><section className="labrika-entry-form"><div className="entry-auth-tabs"><button className={mode==='signup'?'active':''} onClick={()=>{setMode('signup');setError('');}}>Регистрация</button><button className={mode==='login'?'active':''} onClick={()=>{setMode('login');setError('');}}>Войти</button></div><div className="entry-auth-heading"><h2>{mode==='signup'?'Начнём с вашего пространства':'С возвращением'}</h2></div><form onSubmit={async e=>{e.preventDefault();if(busy)return;setBusy(true);setError('');try{if(mode==='signup')await registerAccount({name,email,password});else await loginAccount(email,password);}catch(err){setError(err instanceof Error?err.message:'Не удалось открыть пространство');}finally{setBusy(false);}}}>{mode==='signup'&&<label className="field">Как вас зовут<input required autoComplete="name" value={name} onChange={e=>setName(e.target.value)} placeholder="Ваше имя"/></label>}<label className="field">Почта<input type="email" required autoComplete="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@company.ru"/></label><label className="field">Пароль<div className="entry-password"><input required minLength={8} autoComplete={mode==='signup'?'new-password':'current-password'} type={visible?'text':'password'} value={password} onChange={e=>setPassword(e.target.value)} placeholder="Не менее 8 символов"/><button type="button" aria-label={visible?'Скрыть пароль':'Показать пароль'} onClick={()=>setVisible(!visible)}>{visible?<EyeOff size={17}/>:<Eye size={17}/>}</button></div></label>{error&&<p role="alert" className="entry-auth-error">{error}</p>}<button className="btn btn-primary" disabled={busy} type="submit">{busy?<LoaderCircle className="spin" size={17}/>:mode==='signup'?'Создать пространство':'Войти в пространство'}<ArrowRight size={17}/></button></form>{mode==='signup'&&<p className="entry-agreement">Нажимая «Создать пространство», вы принимаете <a href="#terms" aria-haspopup="dialog" onClick={e=>{e.preventDefault();setLegal('terms');}}>условия использования</a>, соглашаетесь с <a href="#privacy" aria-haspopup="dialog" onClick={e=>{e.preventDefault();setLegal('privacy');}}>политикой конфиденциальности</a> и <a href="#data" aria-haspopup="dialog" onClick={e=>{e.preventDefault();setLegal('data');}}>обработкой данных</a>. <a href="#plans" aria-haspopup="dialog" onClick={e=>{e.preventDefault();setLegal('plans');}}>Тарифы</a></p>}</section></main>{legal&&<LegalDocuments document={legal} onClose={()=>setLegal(null)}/>}</>;
}
